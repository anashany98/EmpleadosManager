# Test credentials for E2E tests
# Copy this file to .env and fill with test credentials

# Admin user for E2E tests
TEST_EMAIL=admin@test.com
TEST_PASSWORD=TestPassword123!

# Skip login tests if no credentials available
SKIP_LOGIN_TESTS=false

# Base URL for tests (default: local dev server)
BASE_URL=http://localhost:5173
